import 'package:flutter/material.dart';

class NewsSingleTile extends StatelessWidget {
  const NewsSingleTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
